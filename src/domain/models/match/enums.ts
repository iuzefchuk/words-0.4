export enum Difficulty {
  High = 'High',
  Low = 'Low',
  Medium = 'Medium',
}

export enum Result {
  Lose = 'Lose',
  Tie = 'Tie',
  Undecided = 'Undecided',
  Win = 'Win',
}

export enum Type {
  Classic = 'Classic',
  Random = 'Random',
}
